// consts
const i18n = require('i18n');

// export
module.exports = {
    name: 'educate-cmd',
    aliases: ['educate', 'edu'],
    example: 'edu',
    execute(client, message, args) {
        const locale = message.author.loc;

        
    },
};